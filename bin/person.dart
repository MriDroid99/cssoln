class Person {
  String id, name, address; // String Value
  int age;

  Person({
    required this.id,
    required this.name,
    required this.age,
    required this.address,
  });
}


// Positional Parameters & Required (Parameters)
// Named Parameters & Optional ({})
// Optional Parameters & Positional ([])